Xbox BIOS ROM files sourced from [ogxbox.co.uk](https://www.ogxbox.co.uk/download/xbox-bios).

The `version` field on each entry records the Xbox dashboard version number associated with that BIOS build. BIOSes v5101 and later are 256KB in size rather than the standard 1MB of the earlier retail builds.
